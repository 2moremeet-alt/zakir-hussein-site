# 🚀 تشغيل موقع "مركز صيانة ذاكر حسين" على Render — دليل كامل خطوة بخطوة

هذا الدليل يأخذك من الصفر حتى يشتغل موقعك على رابط عام يصل إليه أي شخص، مع تفعيل **لوحة التحكم** ورفع الصور بحيث تظهر **للجميع**.

> ⏱️ المدة المتوقعة: **20–30 دقيقة** أول مرة. التكلفة: **صفر جنيه** (خطة Render المجانية).

---

## 📦 الأول: الملفات المطلوبة عندك

افكّ ضغط ملف `zakir-hussein-site.zip` — ستحصل على مجلد `zakir-hussein-site` يحتوي:

| الملف / المجلد | وظيفته |
|---|---|
| `index.html` | صفحة الموقع (كل المحتوى والتصميم) |
| `server.js` | السيرفر الصغير — مسؤول عن **حفظ التعديلات والصور لكل الزوار** |
| `package.json` | يعرّف المنصة أن هذا مشروع Node.js |
| `Procfile` | ملف تشغيل تلقائي (اختياري على Render) |
| `assets/` | اللوجو + نسخة لوحة التحكم |
| `data/` و `uploads/` | **فارغان** — يمتلئان تلقائيًا بالإعدادات والصور بعد التشغيل |
| `دليل-الرفع.txt` / ملف md هذا | التعليمات |

**ملاحظة مهمة:** موقعك ليس مجرد صفحة ثابتة — يحتاج تشغيل `server.js` لكي تعمل ميزة "الحفظ والنشر للجميع" ورفع الصور. لذلك نستخدم Render (وليس رفع ملفات عاديًا).

---

## 🐙 الثاني: إنشاء حساب GitHub ورفع الملفات إليه

Render يعتمد على GitHub لتشغيل الموقع تلقائيًا، لذا نبدأ به:

### 1) إنشاء حساب GitHub (مجاني)
1. افتح موقع <https://github.com>
2. اضغط **Sign up** وأنشئ حسابًا (بريد إلكتروني وكلمة مرور).
3. فعّل الحساب من رابط التأكيد الذي يصلك على البريد.

### 2) إنشاء مستودع (Repository) جديد
1. بعد تسجيل الدخول، اضغط على علامة **+** أعلى يسار الشاشة ثم **New repository**.
2. املأ الحقول هكذا:
   - **Repository name:** `zakir-hussein-site`
   - **Public** (مهم — الخطة المجانية على Render تعمل مع المستودعات العامة)
   - اترك باقي الخيارات كما هي ثم اضغط **Create repository**.

### 3) رفع الملفات (الطريقة الأسهل — من المتصفح)
1. داخل المستودع الجديد اضغط **"uploading an existing file"** (رابط صغير في منتصف الصفحة).
2. افتح مجلد `zakir-hussein-site` و**اسحب كل محتوياته** إلى نافذة المتصفح — أي:
   `index.html`, `server.js`, `package.json`, `Procfile`, `.gitignore`, ومجلدا `assets/` و `data/` و `uploads/` كما هما.
3. اضغط **Commit changes** في الأسفل.

> ✅ أنجزت الآن أول خطوة. يمكنك التأكد من ظهور الملفات داخل المستودع.

---

## 🌐 الثالث: إنشاء حساب Render وربط المستودع

### 1) تسجيل حساب Render
1. افتح <https://render.com>
2. اضغط **Get Started** ثم اختر **"Continue with GitHub"** (الأسرع — لا حاجة لكلمة مرور إضافية).
3. سمّح لـ Render بالوصول إلى حسابك (Authorize) ثم أكمل التسجيل المجاني.

### 2) إنشاء الخدمة (Web Service)
1. من لوحة Render اضغط زر **New +** (أعلى اليسار) ثم اختر **Web Service**.
2. ستظهر قائمة بحسابك GitHub — اختر مستودع `zakir-hussein-site`.
3. ستنتقل إلى شاشة الإعدادات. املأها **بالضبط** كما يلي:

| الحقل | القيمة التي تكتبها |
|---|---|
| **Name** | `zakir-hussein-site` |
| **Region** | `Frankfurt (EU Central)` — الأقرب لمصر |
| **Branch** | `main` |
| **Runtime** | `Node` |
| **Build Command** | `npm install` |
| **Start Command** | `node server.js` |
| **Instance Type** | `Free` |

4. اضغط **Create Web Service** في الأسفل.

### 3) انتظار التشغيل
- ستبدأ عملية النشر (Deploy) وتستغرق **2–5 دقائق** أول مرة.
- شاهد سجل الأحداث (Logs) أسفل الصفحة — ستظهر رسالة نجاح مثل:
  `ZH site server running at http://0.0.0.0:10000`
- عندما يظهر اللون الأخضر **Live** أعلى الصفحة، يصبح موقعك جاهزًا 🎉

---

## ✅ الرابع: الرابط النهائي والتحقق

بعد اكتمال النشر سيظهر لك رابط مشابه:

```
https://zakir-hussein-site.onrender.com
```

### جرّب الآن:
1. افتح الرابط — ستجد الموقع يعمل كاملًا.
2. **افتح لوحة التحكم**: اضغط **5 مرات متتالية** على سطر الحقوق في أسفل الصفحة
   (السطر المكتوب فيه: *© مركز صيانة ذاكر حسين — جميع الحقوق محفوظة*).
3. ستظهر نافذة كلمة المرور — اكتب **`0000`** ثم اضغط "فتح اللوحة".
4. داخل اللوحة يجب أن ترى في الأعلى مؤشرًا أخضر:
   > **● متصل بالسيرفر — الحفظ والنشر لكل الزوار**
5. **غيّر كلمة المرور فورًا**: تبويب **الأمان 🔒** → اكتب كلمة مرور جديدة → **💾 حفظ ونشر للجميع**.
6. جرّب رفع صورة: تبويب **المعرض** → زر **📤 رفع صورة من جهازك** بجانب أي صورة → حفظ.
7. افتح الموقع من **هاتف آخر** أو نافذة **تصفح خاص (Incognito)** — ستجد الصورة والتعديلات ظاهرة للجميع.

---

## 🔁 الخامس: كيف ترفع تحديثات مستقبلية؟

- أي تعديل ترفعه على نفس مستودع GitHub (رفع ملف جديد أو تعديل `index.html`)
  يجعل Render **يعيد النشر تلقائيًا** خلال دقيقة تقريبًا (Auto Deploy).
- تعديلات لوحة التحكم (نصوص/أرقام/صور) لا تحتاج رفعًا جديدًا — تُحفظ فورًا على السيرفر من داخل اللوحة.

---

## ⚠️ السادس: مهم جدًا — حد خطة Render المجانية (اقرئيه)

خطة Render المجانية ممتازة للتجربة، لكن لها قيد واحد يجب أن تعرفيه:

> 🧊 **القرص المؤقت**: على الخطة المجانية، الملفات المكتوبة وقت التشغيل (إعداداتك والصور المرفوعة)
> **غير مضمونة الاستمرار** إذا أُعيد تشغيل الخدمة أو أُعيد نشرها — أحيانًا تُحذف.

بمعنى أبسط: قد تجدين يومًا أن صورك أو تعديلات لوحة التحكم رجعت للوضع الأول.

**الحلول، بالترتيب:**

1. **الحل الجذري (مضمون 100%)**: ترقية الخدمة إلى خطة مدفوعة بسيطة (Starter — حوالي 7–8 دولار شهريًا)
   أو إضافة **Persistent Disk** من إعدادات الخدمة (Settings → Disks).
   عندها تبقى الصور والإعدادات محفوظة **إلى الأبد**.
2. **حل مجاني عملي**: احتفظي دائمًا بنسخة من صورك على جهازك،
   وإذا لاحظتِ اختفاءها بعد فترة، أعدي رفعها من اللوحة (دقيقة واحدة).
3. **قاعدة ذهبية**: أي تغيير كبير في المحتوى احفظيه عندك أيضًا كملاحظة — تحسبًا لأي إعادة ضبط.

> **ونقطة إضافية عن البطء**: الخطة المجانية تُغلق الموقع تلقائيًا بعد 15 دقيقة بدون زوار،
> فأول فتح بعد السكون قد يستغرق **30–60 ثانية** ثم يعمل بسرعة. هذا طبيعي تمامًا.

---

## 🎯 السابع: ربط دومين مخصوص (مثل zh-center.com) — لاحقًا

1. اشتري الدومين من مسجّل مثل Porkbun أو Cloudflare (يكلف سنويًا، غير مجاني).
2. من لوحة Render: افتح خدمتك → **Settings** → **Custom Domain**.
3. أضف الدومين، وسيعطيك Render عنوان **DNS** (A record أو CNAME).
4. انسخه إلى إعدادات DNS عند المسجّل.
5. خلال ساعات، يصبح موقعك على دومينك الخاص، مع إمكانية تفعيل **SSL مجاني** من Render.

---

## ❓ الأسئلة الشائعة

**س: هل أحتاج بطاقة بنكية لخطة Render المجانية؟**
لا — الخطة المجانية لا تطلب بطاقة.

**س: هل يعمل الموقع بدون GitHub؟**
يمكن عبر ميزة **Blueprint** (ملف `render.yaml`)، لكن GitHub هو الطريقة الأسهل والأدق للمبتدئ.

**س: نسيتُ كلمة مرور اللوحة؟**
ارفع ملف `index.html` بعد إزالة خاصية الباسورد من الملف، أو أعد ضبط `data/config.json` على السيرفر، أو اطلب المساعدة.

**س: هل يتغير عنوان الموقع لو حذفت الخدمة وأعدت إنشاءها؟**
نعم — احتفظ بالرابط في مكان آمن، وأفضل حل هو ربط دومين مخصص.

---

## 🏁 الخلاصة

| الخطوة | المكان | الحالة |
|---|---|---|
| فك ضغط الحزمة | جهازك | ✅ |
| حساب GitHub + رفع الملفات | github.com | ⬜ |
| حساب Render + إنشاء Web Service | render.com | ⬜ |
| فتح الرابط وتفعيل اللوحة (0000 ثم تغييرها) | رابط onrender | ⬜ |
| رفع صورة تجريبية والتأكد من ظهورها للجميع | لوحة التحكم | ⬜ |
| (اختياري لاحقًا) دومين مخصوص + ترقية القرص الدائم | Render | ⬜ |

**إذا واجهتك أي مشكلة أثناء أي خطوة، صوّر الشاشة وأرسلها — وسأساعدك فورًا.** 💪
